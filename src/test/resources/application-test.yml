spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        storage_engine: innodb
        globally_quoted_identifiers: true
  h2:
    console:
      enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            scope:
              - email
              - profile